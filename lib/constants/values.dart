import 'package:flutter/material.dart';

//! Border Radius_______________________________________________________________
const kBorderRadius = BorderRadius.all(Radius.circular(15));
const kBorderRadiusSmall = BorderRadius.all(Radius.circular(5));
const kBorderRadiusMedium = BorderRadius.all(Radius.circular(10));
const kBorderRadiusLarge = BorderRadius.all(Radius.circular(30));

//! Padding ____________________________________________________________________
const kPaddingAll = EdgeInsets.all(20);
const kPaddingSmall = EdgeInsets.all(4);

enum LoadingState { loading, loaded, error, hidden }
